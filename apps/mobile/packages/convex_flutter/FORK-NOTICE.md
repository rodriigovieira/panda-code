# This is a modified fork

This directory vendors [convex_flutter](https://github.com/jkuldev/convex_flutter)
v3.0.0 by jkuldev, MIT-licensed (see `LICENSE`, which is retained unmodified as
that licence requires).

**It has been patched for use in Panda Code and is not the upstream release.**
Do not report issues with this copy to the upstream project, and do not assume
its behaviour matches the published package.

The rest of Panda Code is licensed under Apache-2.0 (see the repository root
`LICENSE` and `NOTICE`). This directory keeps its original MIT terms.
